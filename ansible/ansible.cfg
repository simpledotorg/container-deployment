[defaults]
host_key_checking = False
remote_user = "ubuntu"
interpreter_python = /usr/bin/python3

# Fix copy module, source not found error in tmp folder. Github issue: https://github.com/ansible/ansible/issues/31617
remote_tmp = /tmp/
