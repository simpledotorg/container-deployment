loki:
  backend:
    replicas: 3
    extraEnv:
  read:
    replicas: 3
    extraEnv:
  write:
    replicas: 3
    extraEnv:
  minio:
    enabled: true
    persistence:
      size: 10Gi
  loki:
    storage:
      type: s3
