metabase:
  image:
    tag: v0.44.5
  config:
    database:
      type: postgres
    security:
     sessionAge: 1440 # 1 day
  secrets:
    existingSecret:
      name: simple-pguser-metabase
      connectionURIKey: jdbc-uri
