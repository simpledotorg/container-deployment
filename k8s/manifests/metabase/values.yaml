metabase:
  image:
    tag: v0.44.5
  config:
    database:
      type: postgres
  secrets:
    existingSecret:
      name: simple-pguser-metabase
      connectionURIKey: jdbc-uri
  extraEnv:
    MAX_SESSION_AGE: "1440"
