metabase:
  image:
    tag: v0.44.5
  config:
    database:
      type: postgres
  secrets:
    existingSecret:
      name: metabase-pguser-metabase
      usernameKey: user
      passwordKey: password
    database:
      dbName: "metabase"
