{{ if .Values.server.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.server.name }}
  annotations:
    configmap.reloader.stakater.com/reload: {{ .Values.secret_name }}
    secret.reloader.stakater.com/reload: {{ .Values.secret_name }}
spec:
  replicas: {{ .Values.server.replica }}
  selector:
    matchLabels:
      app: {{ .Values.server.name }}
  serviceName: {{ .Values.server.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.server.name }}
    spec:
      containers:
      - name: {{ .Values.server.name }}
        image: {{ .Values.image }}
        args: ["/sbin/my_init"]
        resources:
          {{- toYaml .Values.server.resources | nindent 12 }}
        ports:
          - name: http
            containerPort: {{ .Values.service.port }}
            protocol: TCP
        envFrom:
          - secretRef:
              name: {{ .Values.secret_name }}
          - configMapRef:
              name: {{ .Values.configmap_name }}
        env:
          - name: SIMPLE_SERVER_DATABASE_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ .Values.db_secret.name }}
                key: {{ .Values.db_secret.key }}
                optional: false
        volumeMounts:
        - name: logs
          mountPath: /home/app/log
      {{- with .Values.server.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  volumeClaimTemplates:
  - apiVersion: v1
    kind: PersistentVolumeClaim
    metadata:
      name: logs
    spec:
      accessModes:
      - ReadWriteOnce
      resources:
        requests:
          storage: {{ .Values.server.log.storage }}
      volumeMode: Filesystem
{{ end }}
