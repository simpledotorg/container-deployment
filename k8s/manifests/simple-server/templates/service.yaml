apiVersion: v1
kind: Service
metadata:
  name: simple-server
  labels:
    app: simple-server
spec:
  ports:
  - port: {{ .Values.service.port }}
    protocol: TCP
    targetPort: http
  selector:
    app: simple-server
