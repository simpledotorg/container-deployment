apiVersion: v1
kind: Service
metadata:
  name: simple-server
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app.kubernetes.io/name: simple-server
  sessionAffinity: None
  type: ClusterIP
